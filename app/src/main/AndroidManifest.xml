<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".dagger2_example.UserApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyKotlinBasics">
        <activity
            android:name=".jetpack_archi_compo.viewmodel.QuoteActivity"
            android:exported="false" />
        <activity
            android:name=".jetpack_archi_compo.livedata.LiveDataActivity"
            android:exported="false" />
        <activity
            android:name=".dagger2_example.MainActivity"
            android:exported="false" />
        <activity
            android:name=".jetpack_archi_compo.lifecycle_observer.MainActivity1"
            android:exported="false" />
        <activity
            android:name=".jetpack_archi_compo.room_db.RoomActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>